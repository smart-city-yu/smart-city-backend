spring.application.name=backend

# =====================
# PostgreSQL
# =====================
spring.datasource.url=jdbc:postgresql://localhost:5432/smartcity
spring.datasource.username=smartcity_user
spring.datasource.password=smartcity2026
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# =====================
# Redis (Docker local)
# =====================
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.ssl.enabled=false

# =====================
# JWT
# =====================
jwt.secret=smartcity-super-secret-key-change-this-in-production
jwt.expiration=86400000

# =====================
# Server
# =====================
server.port=8080